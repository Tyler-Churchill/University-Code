#pragma once

#include "Entity.h"

/// \brief CLASS SCRAPPED DUE TO TIME CONTRAINTS
///
class EntityContainer : public Entity
{
  public:
   /// \brief Constructor for Container that creates a Container in the world
   ///
   /// detailed description
   /// \param[in] Entity &entity
   /// \return none
   EntityContainer(Entity &entity);

   /// \brief Function that gives the currently selected Container loot
   ///
   /// detailed description
   /// \param[in] EntityContainer &entity
   /// \return void
   void getLoot(Entity &entity);

   /// \brief Function that checks to see if the Entity contains loot or not
   /// 
   /// detailed description
   /// \param[in] none
   /// \return boolean
   bool hasLoot(); 
};
