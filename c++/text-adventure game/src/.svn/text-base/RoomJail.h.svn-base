#pragma once

#include <string>
#include <vector>
#include <iostream>
#include "Room.h"
//#include "Entity.h"

/// \brief Class RoomJail
///
class RoomJail : public Room
{
  public:

   /// \brief Generates the jail room
   ///
   /// This function generates which enemies will appear in the jail with player
   /// \param[in] none 
   /// \return void 
   void generate();
   
   /// \brief Gets the description for the jail room
   ///
   /// This function tells the user what is in and around the jail 
   /// \param[in] none
   /// \return string-that describes the jail's condition
   std::string getDescription();
   
   /// \brief Checks to see if the room enemies are in the jail room
   ///
   /// This function checks to see if anything is in the jail with player
   /// \param[in] none
   /// \return bool - true if the jail is empty otherwise false
   bool isEmpty();

};
